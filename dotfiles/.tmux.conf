# action key
unbind C-b
set-option -g prefix C-x
set-option -g repeat-time 0

set -ga terminal-overrides ",xterm-256color*:Tc"
#set -g terminal-override "rxvt-unicode*:sitm@,ritm@"
#set -g terminal-override "*-*:Tc"

set-window-option -g mode-keys vi
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# Reload settings
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# vim-like pane switching
# bind -r k select-pane -U 
# bind -r j select-pane -D 
# bind -r h select-pane -L 
# bind -r l select-pane -R 

# # Resizing pane
# bind -r C-k resize-pane -U 5
# bind -r C-j resize-pane -D 5
# bind -r C-h resize-pane -L 5
# bind -r C-l resize-pane -R 5

is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

# navigation
bind-key 'h' if-shell "$is_vim" 'send-keys C-x h'  'select-pane -L'
bind-key 'j' if-shell "$is_vim" 'send-keys C-x j'  'select-pane -D'
bind-key 'k' if-shell "$is_vim" 'send-keys C-x k'  'select-pane -U'
bind-key 'l' if-shell "$is_vim" 'send-keys C-x l'  'select-pane -R'

# resizing
bind-key 'C-h' if-shell "$is_vim" 'send-keys C-x C-h'  'resize-pane -L 1'
bind-key 'C-j' if-shell "$is_vim" 'send-keys C-x C-j'  'resize-pane -D 1'
bind-key 'C-k' if-shell "$is_vim" 'send-keys C-x C-k'  'resize-pane -U 1'
bind-key 'C-l' if-shell "$is_vim" 'send-keys C-x C-l'  'resize-pane -R 1'

# killing
bind 'C-b' if-shell "$is_vim" 'run-shell "tmux send-keys C-x C-b && tmux kill-pane -a"' 'kill-pane -a'
bind '.' if-shell "$is_vim" 'send-keys C-x .' 'kill-pane'

# vim-like moving
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# switching session
bind-key -r -n C-s choose-tree

bind -r C-z split-window -h -p 50
bind -r ö split-window -v -p 50
bind -r - new-window -c "#{pane_current_path}" # really helpful
bind -r n new-window 
bind -r C-u new-window -c "$HOME"
bind -r , kill-window -a
bind -r C-o kill-window
bind -r ü kill-session -a

# bind -r C-b kill-pane -a
# bind -r . kill-pane

#### basic settings

set-option -g status-justify "left"
set-window-option -g mode-keys vi
set-option -g history-limit 64096

set -sg escape-time 10

##set -g status-style 'bg=#333333 fg=#5eacd3'

#set -g status-style 'bg=#0C0D0C fg=#5eacd3'
#set -g status-right '#{=21:pane_title}  #(echo BAT $(acpi --battery | grep -o -E "(..%)"))  %R'

# allow the title bar to adapt to whatever host you connect to
set -g set-titles on
set -g set-titles-string "#T"

# search cheat.sh
bind-key -r i run-shell "tmux neww tmux-cht.sh"

# search stackoverflow using so
bind-key -r C-i run-shell "tmux neww tmux-so.sh"

# search internet using w3m
bind-key -r g run-shell "tmux neww search"

# tmux sessionizer
bind-key -r u run-shell "tmux neww tmux-sessionizer"

# run compile and recompile command in current directory this script is using theprimeagen's tmux-windowizer script
bind-key -r C-m display-popup -E -y 100% -w 100% -h 10% "tmux-compile.sh"
bind-key -r C-n run-shell "tmux-compile.sh -r"

bind -r o last-window
bind -r b switch-client -l
bind -r c copy-mode
